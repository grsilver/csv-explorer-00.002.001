const query = require("./query.js");
const forEach = require ('../lib/forEach.js');
const config = require('../../ssd-explorer.config.js');
module.exports = cr8Table;


function cr8Table(param){


  var sql = ``

   return new Promise(function(resolve,reject){
   })
}
